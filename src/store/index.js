import Vue from 'vue'


// // 自适应计算
// function countratio(w) {
//   let whdef = 100 / 1920;
//   let rem = w * whdef;
//   $('html').css('font-size', rem + "px");
// }

// countratio($(document).width())

// window.addEventListener("resize", () => { //设置高度
//   store.commit('SET_HEIGHT', $(document).height() - 180)
//   countratio($(document).width())
// })



